{
  "mcpServers": {
    "memory": {
      "type": "sse",
      "url": "https://${MEM_HOST:-127.0.0.1}:${MEM_PORT:-7333}/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${MEM_TOKEN:-}"
      }
    }
  }
}
